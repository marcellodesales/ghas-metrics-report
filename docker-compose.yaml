####
#### Attention. First, make a copy of .env-sample as .env to run
#### Fill it out locally so you know
#### $ docker compose up
####
services:
  action:
    image: vionix/ghas-metrics-report
    build:
      context: . 
      #target: os-deps
    volumes:
      - ./dist:/app/dist
    env_file: .env
